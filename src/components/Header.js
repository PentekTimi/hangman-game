// the Header component returns the title and subtitle for the game
function Header() {
    return (
        <div>
            <h1 className="game-title">HANGMAN GAME</h1>
            <h2 className="game-subtitle">Guess the word!</h2>
        </div>
    )
}

// export the Header component
export default Header;